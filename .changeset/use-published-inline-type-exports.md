---
'tersify': patch
---

Replace the inlined `inlineTypeExports` tsdown plugin with the published `@repobuddy/rolldown-inline-type-exports` package. Generated declaration output is unchanged; this only removes the duplicated implementation.
